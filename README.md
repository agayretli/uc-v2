# User communication with Socket.IO
http://localhost:8080
## Running the frontend

```
npm install
npm run serve
```

### Running the server

```
cd server
docker-compose up -d
npm install
npm start
```
